<div class="container">
  <h1>Conversation between <%= @conversation.sender.name %> and <%= @conversation.recipient.name %></h1>

  <% if @over_five %>
    <%= link_to "Show previous", '?m=all', class:'btn btn-secondary' %>
  <% end %>

  <section id="messages" class="mb4">
    <% @messages.each do |message| %>
      <% if message.body %>
        <% user = User.find_by_id(message.user_id) %>
        <article class="message is-dark">
          <div class="message-body">
              <%= gravatar_for user, size: 32, class: "gravatar-img nudge-down-10 pr2" %>
              <strong><%= user.name %></strong> <%= message.message_time %>
            <div class="block pt4">
              <div class="f4"><%= sanitize markdown_to_html(message.body) %></div>
            </div>
          </div>
        </article>
      <% end %>
    <% end %>
  </section>

  <%= form_for [@conversation, @message] do |f| %>
    <%= f.text_area :body, class: "textarea", placeholder: "Inquire about an item..." %>
    <%= f.text_field :user_id, value: current_user.id, type: "hidden"  %>
    <div class="send">
      <%= f.submit "Send message", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>