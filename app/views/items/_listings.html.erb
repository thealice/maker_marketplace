<% items.each do |item| %>
        <div class="item-listing">
            <p><%= item.name %></p>
            <%= link_to (image_tag item.thumbnail, class: "thumbnail", alt: "#{item.name} preview"), shop_item_path(shop, item) %>
        </div>
<% end %>